import PickupLocationsClient from "@/features/fulfillment/pickup/ui/pickup-locations-client";
import React from "react";

const page = () => {
  return <PickupLocationsClient />;
};

export default page;

import { CampaignBuilder } from "@/features/marketing/campaigns/builder";

export const metadata = { title: "New Campaign" };

export default function NewCampaignPage() {
  return <CampaignBuilder />;
}
